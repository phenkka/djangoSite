version: '3.9'

networks:
  dev:

services:
  web:
    build: .
    env_file: ./.env
    container_name: django_backend
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
    networks:
      - dev